file(GLOB_RECURSE SOURCES "src/*.cpp")
add_library(ast STATIC ${SOURCES})
target_include_directories(ast PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
target_link_libraries(ast lexer linker)
add_subdirectory(test)